name: groups
description: A new Flutter project.
version: 0.0.1

publish_to: none

environment:
  sdk: ">=3.4.0 <3.5.0"
  flutter: ">=3.22.0 <3.23.0"

dependencies:
  flutter:
    sdk: flutter

  app_theme:
    path: ../../app_theme

  core:
    path: ../../core

  core_ui:
    path: ../../core_ui

  domain:
    path: ../../domain

  navigation:
    path: ../../navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  auto_route_generator: ^7.3.2
  build_runner: ^2.4.8

flutter:
